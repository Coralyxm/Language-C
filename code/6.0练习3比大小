3.用户从键盘上输入三个整数，找出最大值，然后输出最大值
#include <stdio.h>

int main()
{
    double num1, num2, num3;
    printf("输入三个数:");
    scanf("%f %f %f", &num1, &num2, &num3);
    if (num1 > num2)
    {
        if (num1 > num3)
        {
            printf("max = num1");
        }else
        {
            printf("max = num3");
        }
    }
    if (num2 > num1)
    {
        if (num2 > num3)
        {
            printf("max = num2");
        }else
        {
            printf("max = num3");
        }
    }

    return 0;
}
